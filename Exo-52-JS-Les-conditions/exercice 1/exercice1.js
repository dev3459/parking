let x = 1, y = 2;

/* Modifier le code ci dessous pour réaliser l'exercice */
if(x < y) {
    document.getElementById('monDiv').innerHTML = "La variable x est supérieure à la variable y";
} else if (y > x) {
    document.getElementById('monDiv').innerHTML = "La variable y est supérieure à la variable x";
} else {
    document.getElementById('monDiv').innerHTML = "Les deux variables sont égales";
}



